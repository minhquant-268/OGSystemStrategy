# System Strategy Revise — Architecture

> **Project**: system_strategy_revise (Module 4)
> **Ngay cap nhat**: 2026-03-09
> **Trang thai**: Complete — tat ca module da implement va test

---

## 1. Tong Quan

He thong tinh toan technical indicator va signal giao dich tu du lieu nen.
Nhan du lieu tu Redis (realtime) hoac SQL DB (backfill), chay strategy, day ket qua len Redis DB0 cho Module 5 (OF_ctrader) xu ly lenh.

---

## 2. Cau Truc Thu Muc

```
system_strategy_revise/
├── main.py                         # Entry point — realtime mode
├── back_fill_og.py                 # Entry point — backfill 1000 nen
├── .env                            # Credentials (DB + Redis)
│
├── config/
│   ├── strategy_list.json          # Cau hinh strategy: name, symbols, timeframes
│   ├── output_variable_list.json   # Mo ta cau truc output (fixed + dynamic)
│   └── indicator_list.json         # Danh sach indicator (tham khao)
│
├── src/
│   ├── core/
│   │   ├── strategy_engine.py      # Dieu phoi: load config, filter df, goi strategy
│   │   ├── keyspace_monitor.py     # Lang nghe Redis Keyspace event
│   │   ├── redis_publisher.py      # Ghi OG/signal hash + pub/sub
│   │   └── cleanup_worker.py       # Background thread doc dep du lieu cu
│   │
│   ├── strategies/
│   │   ├── base_strategy.py        # Abstract base class
│   │   ├── strategy_comboATR.py    # ComboATR (MACD + SMA + ATR)
│   │   ├── strategy_MAcrossover.py # MA Crossover + ATR
│   │   └── strategy_RSI.py         # RSI + ATR
│   │
│   └── utils/
│       ├── logger.py               # Logging (RotatingFileHandler + UTC)
│       ├── helpers.py              # (placeholder — chua dung)
│       ├── connection/
│       │   ├── db_connect.py       # SQL Server connection + get_candles()
│       │   └── redis_connect.py    # Redis client + helper functions
│       └── indicators/
│           ├── MA.py               # SMA, EMA
│           ├── MACD.py             # MACD, Signal, Histogram
│           ├── ATR.py              # Average True Range
│           ├── RSI.py              # Relative Strength Index
│           ├── BollingerBands.py   # BB + %B + Bandwidth
│           └── ADX.py             # ADX + DI+/DI-
│
├── tests/
│   ├── test_indicators.py          # 5 tests — MA, MACD, ATR, RSI
│   ├── test_strategies.py          # 39 tests — 3 strategies + engine
│   └── test_connection.py          # Test ket noi DB + Redis
│
└── docs/
    ├── 0.system_strategy_architecture.md  # (file nay)
    ├── 1.module_connection.md
    ├── 2.module_indicators.md
    ├── 3.module_strategies.md
    ├── 4.module_core.md
    ├── 5.module_main.md
    ├── 6.module_backfill.md
    ├── README.md
    └── requirements.txt
```

---

## 3. Dependency Graph

```
main.py ─────────────────────────────── back_fill_og.py
  │                                        │
  ├── logger.py                            ├── logger.py
  ├── strategy_engine.py ◄─────────────── ├── strategy_engine.py
  ├── keyspace_monitor.py                  ├── db_connect.py (query SQL)
  │     ├── redis_connect.py               ├── redis_connect.py
  │     ├── strategy_engine.py             └── redis_publisher.py
  │     └── redis_publisher.py                  └── redis_connect.py
  │           └── redis_connect.py
  └── cleanup_worker.py
        └── redis_connect.py

strategy_engine.py
  └── strategy_list.json (config)
        └── strategy_comboATR.py ──┐
        └── strategy_MAcrossover.py ├── indicators/*.py
        └── strategy_RSI.py ───────┘
```

---

## 4. Data Flow

### 4.1 Realtime Mode (main.py)

```
Module 2 ghi nen moi len Redis
    │  HSET candle:CAPITALCOM:US30:10:2026-03-09 00:00:00
    v
keyspace_monitor.py
    │  Subscribe __keyspace@0__:candle:*
    │  Parse key → provider, symbol, timeframe
    │  Doc bucket nen tu Redis (get_candle_bucket)
    │  Build DataFrame OHLCV
    v
strategy_engine.py
    │  Load strategy_list.json
    │  Filter df theo (symbol, timeframe) cua tung strategy
    │  Goi strategy.calculate_signals(df_filtered)
    v
redis_publisher.py
    │  Ghi OG hash   → OG:{strategy}:{provider}:{symbol}:{tf}:{dt}
    │  Ghi signal hash → signal:{strategy}:{provider}:{symbol}:{tf}:{dt}
    │  Publish channel → signals_channel:{strategy}:{provider}:{symbol}:{tf}
    │  Maintain list  → OG:{...} va signal:{...}
    v
Module 5 (OF_ctrader) lang nghe channel → dat lenh
```

### 4.2 Backfill Mode (back_fill_og.py)

```
SQL Server (dbo.providers → lay tat ca provider)
    │  query dbo.providers
    v
back_fill_og.py
    │  Doc strategy_list.json → lay (symbol, timeframe)
    │  Loop: provider × symbol × timeframe
    │  get_candles(symbol, tf, provider, limit=1000)
    v
strategy_engine.py → redis_publisher.publish_backfill()
    │  Ghi TOAN BO nen len Redis (pipeline)
    │  Trim list max 1000
    v
Redis DB0 co san du lieu lich su
```

---

## 5. Config Files

| File | Dung boi | Muc dich |
|---|---|---|
| `.env` | `db_connect.py`, `redis_connect.py` | Credentials (DB_SERVER, REDIS_HOST, ...) |
| `strategy_list.json` | `strategy_engine.py` | Strategy nao enabled, cho symbol/tf nao |
| `output_variable_list.json` | (tham khao) | Mo ta cau truc output Redis hash |
| `indicator_list.json` | (tham khao) | Danh sach indicator |

---

## 6. Redis Key Convention (DB0)

```
Candle (input - do Module 2 ghi):
  candle:{provider}:{symbol}:{tf}:{datetime}     → Hash (OHLCV)

OG (output - tat ca nen):
  OG:{strategy}:{provider}:{symbol}:{tf}:{datetime}     → Hash (payload)
  OG:{strategy}:{provider}:{symbol}:{tf}                 → List [datetime, ...]

Signal (output - chi nen co signal):
  signal:{strategy}:{provider}:{symbol}:{tf}:{datetime}  → Hash (payload)
  signal:{strategy}:{provider}:{symbol}:{tf}              → List [datetime, ...]

Pub/Sub Channel:
  signals_channel:{strategy}:{provider}:{symbol}:{tf}    → JSON message
```

---

## 7. Test Coverage

| Test File | So test | Covers |
|---|---|---|
| `test_indicators.py` | 5 | MA, MACD, ATR, RSI |
| `test_strategies.py` | 39 | ComboATR, MACrossover, RSI, StrategyEngine |
| **Total** | **44** | **All passed** |
