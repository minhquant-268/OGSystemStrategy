# ============================================================
# System Strategy Revise - Requirements
# ============================================================
# Cai dat: pip install -r docs/requirements.txt
# ============================================================

# ── Core Data ────────────────────────────────────────────────
pandas>=2.0.0               # DataFrame, Series - dung trong tat ca indicator, strategy
numpy>=1.24.0               # Tinh toan so hoc - dung trong ATR, RSI, MACD, strategy loop

# ── Database (SQL Server) ────────────────────────────────────
pyodbc>=4.0.0               # ODBC driver Python - ket noi truc tiep SQL Server
sqlalchemy>=2.0.0           # ORM + engine - dung trong db_connect.py
                            # pip install sqlalchemy[pyodbc]

# ── Redis ────────────────────────────────────────────────────
redis>=5.0.0                # Redis client - dung trong redis_connect.py
                            # Bao gom: redis.Redis, PubSub, scan, hset, lpush, publish

# ── Config / Environment ─────────────────────────────────────
python-dotenv>=1.0.0        # Doc .env file - load_dotenv() trong db_connect va redis_connect

# ── Testing ──────────────────────────────────────────────────
pytest>=7.0.0               # Test framework - chay: pytest tests/test_strategies.py -v
pytest-cov>=4.0.0           # Coverage report - chay: pytest --cov=src tests/

# ── Ghi chu ─────────────────────────────────────────────────
# Cac thu vien sau la built-in Python, KHONG can cai:
#   os, sys, logging, typing, json, importlib, dataclasses
#
# Phien ban da kiem tra (2026-03-07):
#   pandas       3.0.0
#   numpy        2.4.2
#   pyodbc       5.2.0
#   sqlalchemy   2.0.45
#   redis        6.2.0
#   python-dotenv 1.2.1
#
# Thu vien tuy chon (bo comment neu can):
# aioodbc>=0.5.0            # Async ODBC - neu muon async DB query
# pymysql>=1.0.0            # MySQL driver - neu doi sang MySQL
