# ============================================================
# System Strategy Revise - Requirements
# ============================================================
# Cai dat: pip install -r docs/requirements.txt
# ============================================================

# ── Core Data ────────────────────────────────────────────────
pandas>=2.0.0               # DataFrame, Series - dung trong tat ca indicator va get_candles()
numpy>=1.24.0               # Tinh toan so hoc - dung trong ATR, RSI, ADX, Bollinger

# ── Database (SQL Server) ────────────────────────────────────
pyodbc>=4.0.0               # ODBC driver Python - ket noi truc tiep SQL Server
sqlalchemy>=2.0.0           # ORM + engine - dung trong db_connect.py (create_engine, text)
                            # pip install sqlalchemy[pyodbc]

# ── Redis ────────────────────────────────────────────────────
redis>=5.0.0                # Redis client - dung trong redis_connect.py
                            # Bao gom: redis.Redis, PubSub, scan, hset, lpush, publish

# ── Config / Environment ─────────────────────────────────────
python-dotenv>=1.0.0        # Doc .env file - load_dotenv() trong db_connect va redis_connect

# ── Testing ──────────────────────────────────────────────────
pytest>=7.0.0               # Test framework - chay tests/
pytest-cov>=4.0.0           # Coverage report - pip install pytest-cov
                            # Chay: pytest --cov=src tests/

# ── Ghi chu ─────────────────────────────────────────────────
# Cac thu vien sau la built-in Python, KHONG can cai:
#   os, sys, logging, traceback, typing, urllib.parse, json, io
#
# Thu vien tuy chon (bo comment neu can):
# aioodbc>=0.5.0            # Async ODBC - neu muon async DB query
# pymysql>=1.0.0            # MySQL driver - neu doi sang MySQL
